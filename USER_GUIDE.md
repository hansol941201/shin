# 사용자 가이드

## 좋은 결과물을 위한 입력 PPT 기준

- 같은 공종(예: 재도장)의 PPT 2~3개를 사용하세요. 서로 다른 공종을 섞으면 공법 순서/특징이
  뒤섞여 품질이 떨어집니다.
- 사진은 해상도가 높을수록 좋습니다(짧은 변 120px 미만은 아이콘으로 간주되어 자동 배제됩니다).
- "시공 전"/"시공 후" 문구가 사진과 같은 슬라이드에 텍스트로 함께 있어야 전후 비교 사례로
  인식됩니다. (프로그램은 서로 다른 슬라이드/파일의 사진을 임의로 전후 관계로 엮지 않습니다.)
- 공정 사진에는 "고압세척", "퍼티", "하도", "중도", "상도" 등 공정명을 알 수 있는 문구를
  슬라이드 제목이나 캡션에 넣어 두면 자동 분류 정확도가 올라갑니다.
- 회사 로고/현수막/명함 등은 넣지 않는 것이 가장 안전합니다. 프로그램이 최대한 탐지해 배제하지만,
  탐지가 확실하지 않을 경우 해당 사진 전체를 사용하지 않는 보수적 방식으로 동작합니다.
- 가로형 슬라이드가 섞여 있어도 텍스트/사진 추출 자체는 가능하지만, 사진 비율이 가로로 긴
  경우 최종 세로형 레이아웃에서 크게 쓰기 어려울 수 있습니다.

## GUI 사용 순서

1. `python run_app.py` 실행 (또는 빌드된 `AutoMaterialCollector.exe` 실행)
2. 새 아파트명 입력
3. 공종 선택
4. 기존 PPT 파일 2~3개 추가 (드래그 앤 드롭 또는 "+ 파일 추가" 버튼)
5. 출력 폴더 확인/변경
6. "새 자료 만들기" 클릭
7. 진행 상태(분석 중 → 이미지 추출 중 → … → 완료)를 확인
8. 완료 후 "결과 PPT 열기" 또는 "저장 폴더 열기"로 확인, "다시 만들기"로 새 작업 시작

## 아직 자동 판별이 어려운 항목 (수동 확인 권장)

1. **이미지 내부 텍스트(OCR) 인식률** — 사진 속 아파트명/현수막/표지판 문구는 각도, 해상도,
   폰트, 조명에 따라 OCR이 놓칠 수 있습니다. 자동 검수 보고서의 "수동 확인 필요" 항목과 최종
   PPT를 반드시 육안으로 한 번 더 확인하세요.
2. **얼굴이 포함된 사진** — 사람 얼굴 식별 여부는 자동으로 판단하지 않습니다. 사람이 크게 나온
   사진은 입력 단계에서 제외하는 것을 권장합니다.
3. **공정 순서가 자료마다 다르게 표기된 경우** — "사용자 확인 필요" 단계로 표시만 하고 임의로
   순서를 정하지 않습니다. 실제 공정과 대조해 확정해 주세요.
4. **텍스트 잘림/자연스러운 문장 흐름** — 자동 생성된 캡션·설명 문구가 상황에 따라 다소 기계적일
   수 있습니다. 필요 시 PowerPoint에서 직접 미세 조정하세요.
5. **전후 사진의 실제 동일 지점 여부** — 같은 슬라이드 내 "시공전/시공후" 라벨을 기준으로 짝을
   짓지만, 라벨이 없는 자료에서는 짝을 짓지 않고 페이지 자체를 생략합니다.
6. **사진 내 QR코드/서명/도장 등 소형 그래픽** — 해상도가 매우 낮거나 극단적인 종횡비인 경우는
   배너/로고로 간주해 배제하지만, 정상 크기의 QR코드 등은 별도 이미지 인식 없이 텍스트(OCR)
   기반으로만 탐지되므로 완벽하지 않습니다.

## 자동 검수 보고서 읽는 법

`[아파트명]_검수결과.txt` 파일은 세 구간으로 나뉩니다.

- **통과**: 문제없이 확인된 항목
- **자동 수정**: 프로그램이 스스로 고친 항목(예: 슬라이드 밖으로 나간 요소 재배치)
- **수동 확인 필요**: 프로그램이 고칠 수 없어 사람이 확인해야 하는 항목(예: 반복 사진,
  저해상도 확대 의심)

수동 확인 항목이 있다면 결과 PPT를 열어 해당 페이지를 반드시 검토한 뒤 배포하세요.
