-- ============================================================================
-- 0006_pour_source_ref.sql
-- 엑셀 원본 위치와 겹침 표시를 담을 열 두 개 (순방향)
--
-- 0005_pour_record_source.sql 다음에 실행합니다.
--
-- 담기는 값
--   source_ref   "2025년 348행" — 연도별 실적 List 의 어느 시트 몇 번째 줄에서 왔는지
--   duplicate_of "rec-imp-0061" — 겹치는 줄이면 먼저 나온 줄의 id (아니면 NULL)
--
-- 쓰임
--   모든 시트의 모든 줄을 하나도 빼지 않고 옮기되, 겹치는 줄을 지우지 않고
--   표시만 남기기 위해서입니다. 어느 행이든 원본으로 되짚을 수 있습니다.
--
-- 원칙
--   · 열만 더합니다. 기존 열·행을 지우거나 바꾸지 않습니다
--   · DROP / DELETE / TRUNCATE / 표 교체를 쓰지 않습니다
-- ============================================================================

ALTER TABLE projects ADD COLUMN source_ref TEXT;
ALTER TABLE projects ADD COLUMN duplicate_of TEXT;
