-- ============================================================================
-- 0005_pour_record_source.sql
-- 자료가 어디서 들어왔는지 표시할 record_source 열 (순방향)
--
-- 0004_pour_category_items.sql 다음에 실행합니다.
--
-- 담기는 값
--   "import" — 연도별 실적 List 엑셀에서 옮겨 온 행
--   NULL     — 프로그램에서 등록한 행 (기존 행도 그대로 NULL 입니다)
--
-- 쓰임
--   엑셀 이전분에는 협약서 발행번호가 아예 없습니다. 이 행들까지 알림에 넣으면
--   "협약서번호 미입력" 이 1,900건을 넘겨 정작 봐야 할 것이 묻힙니다.
--   그래서 그 알림은 프로그램에서 등록·전환한 건만 대상으로 합니다.
--   번호를 지어내거나 고치지 않으며, 상세 수정 화면에서 직접 넣는 길은 그대로 둡니다.
--
-- 원칙
--   · 열 하나만 더합니다. 기존 열·행을 지우거나 바꾸지 않습니다
--   · DROP / DELETE / TRUNCATE / 표 교체를 쓰지 않습니다
-- ============================================================================

ALTER TABLE projects ADD COLUMN record_source TEXT;
