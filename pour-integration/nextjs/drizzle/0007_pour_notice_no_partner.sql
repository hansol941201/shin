-- ============================================================================
-- 0007_pour_notice_no_partner.sql
-- K-APT 공고 → 낙찰 업무 흐름에 필요한 열 두 개 (순방향)
--
-- 0006_pour_source_ref.sql 다음에 실행합니다.
--
-- 담기는 값
--   notice_no  "2026-0001" — K-APT 공고번호. 공고 단계에서 확인되는 값입니다.
--   is_partner "예" / "아니오" / 빈 값(아직 확인 안 됨) — 협약사 여부.
--              협약서 발행번호(agreement_no)와는 뜻이 다르므로 따로 둡니다.
--
-- 쓰임
--   한 현장을 공고 등록 → 낙찰 변경까지 같은 행에서 이어 관리하기 위해,
--   공고 단계 값(notice_no)과 낙찰 뒤 확인되는 값(is_partner)을 함께 담습니다.
--
-- 원칙
--   · 열만 더합니다. 기존 열·행을 지우거나 바꾸지 않습니다
--   · 기존 자료의 두 열은 NULL 로 남습니다 (값을 임의로 만들지 않습니다)
--   · DROP / DELETE / TRUNCATE / 표 교체를 쓰지 않습니다
-- ============================================================================

ALTER TABLE projects ADD COLUMN notice_no TEXT;
ALTER TABLE projects ADD COLUMN is_partner TEXT;
