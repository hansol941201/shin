{
  "_comment": "이 저장소는 GitHub Pages로 정적 서빙되므로(hosting 설정 없음), 이 파일은 Cloud Functions(AI 문구정리 서버 함수) + Storage 보안 규칙 배포용으로만 쓴다. 배포 전 이전 라운드의 firestore.rules/storage.rules를 덮어쓰지 않았는지 확인할 것. storage.rules는 v2/storage.rules를 그대로 가리킨다(자료실 라운드에서 이미 그 경로에 만들어져 있던 파일을 그대로 확장해 쓴 것 — 새 파일을 루트에 중복 생성하지 않았다).",
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ]
    }
  ],
  "storage": {
    "rules": "v2/storage.rules"
  }
}
