{
  "_comment": "이 저장소는 GitHub Pages로 정적 서빙되므로(hosting 설정 없음), 이 파일은 오직 Cloud Functions(AI 문구정리 서버 함수) 배포용으로만 새로 추가했다. 배포 전 이전 라운드의 firestore.rules/storage.rules를 덮어쓰지 않았는지 확인할 것 — 이번 라운드 이전에는 firebase.json 자체가 저장소에 없었다(신규 파일).",
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ]
    }
  ]
}
