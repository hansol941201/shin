# Legacy 엔진 (v1, 보존용)

고정 15페이지 템플릿 + 카테고리당 소수 대표 사진 선정 방식의 이전 생성 엔진입니다.
사진/문구 활용도가 낮다는 실제 피드백에 따라 `app/engine/` 의 신규 콘텐츠 중심 엔진으로 대체되었습니다.
더 이상 기본 파이프라인에서 사용되지 않으며, 참고용으로만 보존합니다.

- `slide_planner/` — 고정 페이지 구조 계획 (구 버전)
- `ppt_generator/` — 고정 레이아웃 렌더러 (구 버전)
- `selector_v1.py` — 카테고리당 소수 대표 사진만 뽑던 선택 로직 (구 버전)
